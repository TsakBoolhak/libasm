#ifndef LIBASM_TESTER_BONUS_H
# define LIBASM_TESTER_BONUS_H
# include <libasm_tester.h>

int	    atoiBaseTester(int argCount, char **argVector);
int	    listTester(int argCount, char **argVector);

#endif